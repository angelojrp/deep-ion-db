node_modules/
out/
dist/
*.log
*.tsbuildinfo
.DS_Store
.vscode/
.idea/

# Claude Code — configurações pessoais (não versionar)
.claude/settings.local.json

# Segredos de deploy
.env
deploy/k8s/secret.yaml

# Frontend web (build) — gerados pelo esbuild (scripts/build-web.mjs).
# app.css é o bundle das CSS importadas em web/src/main.tsx; editar a fonte
# (styles.css / web/src/admin.css), nunca o artefato gerado.
web/public/app.js
web/public/app.css

# Playwright — artefatos de execução (não versionar)
test-results/
playwright-report/
